# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# uv / virtualenv
.venv/

# Secrets (PRD NFR-3: keys via env only, never committed)
.env

# Cloned target repo + generated artifacts (external / reproducible, not committed)
data/
graphify-out/

# OS / editor
.DS_Store
.idea/
.vscode/

# Obsidian per-vault local config / workspace state (not a deliverable)
.obsidian/

# Loose figures saved by the results notebook (canonical copies are embedded in the .ipynb)
notebooks/*.png
